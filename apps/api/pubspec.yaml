name: forgottenlandapi
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  args: ^2.0.0
  dio: ^5.1.1
  shelf: ^1.1.0
  shelf_router: ^1.0.0
  supabase: ^1.2.0
  models:
  utils:

dev_dependencies:
  build_runner: ^2.3.2
  build_web_compilers: ^3.2.7
  http: ^0.13.0
  lints: ^2.0.0
  test: ^1.15.0
