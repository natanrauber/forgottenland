name: utils
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  dio: ^5.1.1
  supabase: ^1.2.0
  models:

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
